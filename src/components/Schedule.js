import React, { Component } from 'react';
import { Card } from '@material-ui/core';

export default class Schedule extends Component {
  render() {
    return (
      <div>
        <p> this is for schedules </p>
      </div>
    );
  }
}
